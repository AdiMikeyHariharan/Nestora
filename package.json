{
  "name": "nestora",
  "version": "1.0.0",
  "private": true,
  "description": "Nestora — real estate platform (Node + Postgres API, React client)",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm run build",
    "dev:client": "cd client && npm run dev"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
